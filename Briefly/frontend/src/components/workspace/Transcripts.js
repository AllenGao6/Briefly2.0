import React from "react";
import { Grid } from "@material-ui/core";

export default function Transcripts() {
  return (
    <Grid
      container
      style={{
        zIndex: 1,
        background: "white",
        position: "absolute",
        top: 0,
        bottom: 0,
      }}
    >
      Transcript Section
    </Grid>
  );
}
